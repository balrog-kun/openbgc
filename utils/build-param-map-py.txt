/* vim: set ts=4 sw=4 sts=4 et : */
#define hash #
hash Autogenerated from src/param-defs.c.inc

from enum import IntFlag, auto
class ParamFlag(IntFlag):
    store = auto()
    ro = auto()
    ptr = auto()
    setter = auto()
    const = auto()

import collections
ParamIdInfo = collections.namedtuple('ParamIdInfo', [ 'enum_id', 'name', 'flags' ])

ParamDef = collections.namedtuple('ParamDef', [ 'id', 'name', 'size', 'typ', 'flags' ])

#define PARAM_DEF(data)                 PARAM_DEF_FULL(data, ParamFlag(0))
#define PARAM_DEF_SAVE(data)            PARAM_DEF_FULL(data, ParamFlag.store)
#define PARAM_DEF_RO(data)              PARAM_DEF_FULL(data, ParamFlag.ro)
#define PARAM_DEF_PTR(_ptr, offset)     PARAM_DEF_FULL(_ptr->offset, ParamFlag.ptr)
#define PARAM_DEF_PTR_RO(_ptr, offset)  PARAM_DEF_FULL(_ptr->offset, ParamFlag.ptr | ParamFlag.ro)
#define PARAM_DEF_SETTER(data, _setter) PARAM_DEF_FULL(data, ParamFlag.setter)
#define PARAM_DEF_CONST(value)          PARAM_DEF_FULL(value, ParamFlag.const | ParamFlag.ro)

#include "../src/util.h"
#define INDENT
#define PARAM_DEF_FULL(data, flags) \
    INDENT ParamIdInfo(STRINGIFY(CONCAT(param_, __LINE__)), STRINGIFY(data), flags),

param_map = [
#include "../src/param-defs.c.inc"
]
